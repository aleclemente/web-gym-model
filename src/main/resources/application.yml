spring:
  datasource:
    url: jdbc:h2:file:/C/Users/Alexandre Clemente/OneDrive/Documentos/projetos/banco
    username: sa
    password: password
    driverClassName: org.h2.Driver
  h2:
    console.enable: true
  jpa:
    show-sql: true
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate.format_sql: true
